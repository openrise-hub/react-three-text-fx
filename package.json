{
  "name": "react-three-text-fx",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run -w apps/demo dev",
    "build": "npm run -w packages/core build && npm run -w apps/demo build",
    "build:core": "npm run -w packages/core build",
    "build:demo": "npm run -w apps/demo build"
  },
  "dependencies": {
    "three-bas": "^3.0.0"
  }
}
